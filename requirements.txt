# Core dependencies for static site generator
markdown>=3.7,<4
jinja2>=3.1.5,<4
pyyaml>=6.0.2,<7

# Development dependencies (install with: pip install -r requirements.txt -r requirements-dev.txt)
# Or use: pip install -e ".[dev]" if using pyproject.toml
